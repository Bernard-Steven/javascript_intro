

//lambda
function sqr(x){
    return x*x
}

let aSqr = (x) => {
    return x * x //explicit return
}
//implicit
let bSqr = (x) => x * x
let cSqr =  x => x * x //only works without () beacuse single variable


let aFunctionName = (fname, lname) => ${fname} ${lname}

//hoisting

